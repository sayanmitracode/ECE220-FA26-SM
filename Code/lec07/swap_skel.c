/* Does this swap main's x and y? */
#include <stdio.h>

void swap(int x, int y) {
    /* declare a temporary */

    /* three assignments to exchange x and y */

}

int main() {
    int x = 2, y = 3;
    swap(x, y);
    printf("x = %d, y = %d\n", x, y);
    return 0;
}
