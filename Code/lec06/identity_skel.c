/* Print an N x N identity matrix with nested loops */
#include <stdio.h>
#define N 5

int main() {
    /* declare loop counters */

    /* outer loop: one pass per row */

        /* inner loop: one pass per column */

            /* print 1 on the diagonal, 0 elsewhere */

        /* end the row with a newline */

    return 0;
}
